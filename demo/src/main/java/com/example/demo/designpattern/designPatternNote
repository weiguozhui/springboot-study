设计模式笔记

工厂模式
    普通工厂模式
        定义一个一系列类的顶层接口，其实例实现该接口
        定义实现类
        定义工厂类，提供一个静态的获取实例的方法，方法返回值为顶层接口，通过传参的方式来确定具体实例化哪个类

                    share接口               工厂类
                       |                      |
                       |                      |
                   -----------                |
                  |           |               |
                  实现类1  实现类2-------------get方法--返回share接口，根据传参确定具体返回的实现类